#include <iostream>

using namespace std;

int main (){
    cin.tie(0);
    ios::sync_with_stdio(false);

    int a, b, c;
    string s;

    cin >> a >> b >> c >> s;

    cout << a+ b + c << ' ' << s << '\n';
}